function roll(w, x, y, z)
    sinr_cosp = 2 * (w * x + y * z)
    cosr_cosp = 1 - 2 * (x * x + y * y)
    return math.atan2(sinr_cosp, cosr_cosp)
end

return roll(
    get("orientation.w"),
    get("orientation.x"),
    get("orientation.y"),
    get("orientation.z")
)